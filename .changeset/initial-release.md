---
"theialang": minor
---

Initial public release of the `theialang` CLI: compile `.theia` lectures into
self-contained, interactive HTML slide decks (live sliders, reactive KaTeX,
2D/3D scenes, equation morphing, code cells). Ships standalone with the KaTeX
assets and the Theia reactive runtime baked in — no runtime dependencies.
