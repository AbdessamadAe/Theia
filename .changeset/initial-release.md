---
"chalkdeck": minor
---

Initial public release of the `chalkdeck` CLI: compile `.chalk` lectures into
self-contained, interactive HTML slide decks (live sliders, reactive KaTeX,
2D/3D scenes, equation morphing, code cells). Ships standalone with the KaTeX
assets and the Chalk reactive runtime baked in — no runtime dependencies.
